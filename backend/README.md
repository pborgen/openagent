# OpenAgent Backend (Go)

This is a minimal local service that will:
- Load config from repo or user home
- Accept run requests
- Stream logs

Planned endpoints:
- GET /health
- POST /run
- GET /logs/stream

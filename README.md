# OpenAgent

Cursor extension to run multi-agent workflows (LangGraph first) and manage configs.

## MVP scope
- Run workflow + show logs
- Manage configs/env

## Next steps
- Add backend runner (Python LangGraph) + bridge (local server)
- Add log/trace view (tree + timeline)

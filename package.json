{
  "name": "openagent-monorepo",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "build:extension": "pnpm --dir apps/extension compile",
    "build:backend": "pnpm --dir apps/backend build",
    "build": "pnpm build:extension && pnpm build:backend"
  }
}
